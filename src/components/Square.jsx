const Square = ({ color }) => {
  return (
    <div
      style={{
        width: "100px",
        height: "100px",
        backgroundColor: color,
        marginTop: "20px",
      }}
    ></div>
  );
};

export default Square;
